import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "EduPhysMath | International Physics & Mathematics Tutoring",
  description:
    "Expert online tutoring in Physics and Mathematics for IB, AP, IGCSE, and A-Level curricula. Personalized sessions with Dr. Abdul Rauf, PhD Physics.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <body className="min-h-full flex flex-col antialiased">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
